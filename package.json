{
  "name": "glass-detection-api",
  "version": "1.0.0",
  "description": "API for detecting broken glass using Teachable Machine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^3.18.0",
    "@teachablemachine/image": "^0.8.5",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "multer": "^1.4.4",
    "canvas": "^2.11.2"
}
  },
  "engines": {
    "node": "18.x"
  }
}
